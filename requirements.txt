fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.5
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# passlib 1.7.x is unmaintained and breaks under bcrypt>=4.1 (it self-tests
# with a >72-byte dummy password, which newer bcrypt now rejects outright
# instead of silently truncating like older bcrypt did). Pin to the last
# version passlib was actually tested against.
bcrypt==4.0.1
python-multipart>=0.0.9
alembic>=1.13
pytest>=8.3
httpx>=0.27
Pillow>=10.4
numpy>=1.26
python-dotenv>=1.0
requests>=2.32

